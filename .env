# ──────────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT env file.
# This file must NEVER be committed to git (it's already in .gitignore).
# When you deploy to Render/Railway, you will re-enter these same
# keys (with production values) into their dashboard's environment
# variables section — you do not upload this file itself.
# ──────────────────────────────────────────────────────────────

# A freshly generated secret key — safe to use, only you have it.
# If you ever accidentally commit this file, generate a new one with:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=X1S=(etl_v6^7p&kb8j8j8%bmuxh1y%t403o-nmm$e-$w1kr(!avrm

# Keep DEBUG=True for local development so you can see error pages.
# Set this to False in production (Render/Railway dashboard).
DEBUG=False

ALLOWED_HOSTS=
CSRF_TRUSTED_ORIGINS=

# Leave DATABASE_URL blank locally — settings.py falls back to SQLite
# automatically so you can keep developing without Postgres installed.
DATABASE_URL= postgresql://portfolio_db_h3u4_user:s3Xr5nvfnjzH9uHxRby2FSbeZVhsTzIC@dpg-daau0rek1f9s73esua40-a/portfolio_db_h3u4

# Fill these in after creating a free account at cloudinary.com
# (Dashboard → Account Details). Leave blank to skip Cloudinary
# locally, but then profile_image / resume_file uploads will error —
# fill these in before testing uploads.
CLOUDINARY_CLOUD_NAME= dbrolrapw
CLOUDINARY_API_KEY= 638383785118818
CLOUDINARY_API_SECRET= E7KnewoZhuRHGk3UX2IwMqtGBHM

# DATABASE_URL=postgresql://portfolio_db_h3u4_user:s3Xr5nvfnjzH9uHxRby2FSbeZVhsTzIC@dpg-daau0rek1f9s73esua40-a.oregon-postgres.render.com/portfolio_db_h3u4